import networkx as nx
# from sondao.GUI.sondao_web.gentree.models import Person, Relation
from sondao.logic.RelationTypes import RelationTypes


class Algorithm:

    def find_heir(self):
        """

        :return:
        """
        # menager = Person.objects
        # items = menager.all()
        # graph = nx.Graph()
        # for item in items.values():
        #     graph.add_node(item['id'],
        #                    label=f"{item['name']} {item['surname']}",
        #                    physics=False,
        #                    shape="box")
        # print(RelationTypes.choices())
        # print(RelationTypes.CHILD.value - 1)
